# Пользовательские сценарии и UX-контракт

Статус: основные ежедневные пользовательские сценарии реализованы и закрыты browser release-gates.  
Версия: `0.1.0-rc.5`.

## Принципы

Интерфейс проектируется не как административная база, а как ежедневное рабочее место оператора.

1. **Сразу к основной задаче.** После входа открывается календарь текущего месяца.
2. **Одно место — одно назначение.** Переключатель `Месяц / Неделя / Задачи` меняет только представление календаря и не переносит пользователя в другой раздел.
3. **Предсказуемое создание.** Кнопка `Создать` всегда открывает один и тот же лист с событием или задачей.
4. **Статус рядом с объектом.** Загрузка, обработка, ошибка и необходимость проверки показываются непосредственно у документа.
5. **Не блокировать работу.** Фоновая обработка не удерживает модальное окно и не мешает календарю, поиску и другим документам.
6. **Редкие прерывания.** Критические диалоги используются только для необратимых действий. Информация и успех показываются в контексте или коротким уведомлением.
7. **Восстановимость.** Закрытие листа не меняет данные; выполненную задачу можно вернуть в работу.
8. **Одинаковая терминология.** В интерфейсе используются слова `событие`, `задача`, `напоминание`, `документ`, `шаблон`, `поле`, `проверка`.
9. **Доступность.** Все действия доступны с клавиатуры; состояние не кодируется одним цветом; поддерживается уменьшение движения.
10. **Адаптация, а не уменьшенная копия.** На мобильном экране боковая панель заменяется нижними вкладками, а листы открываются снизу.
11. **Обучение без перестройки.** Повторяющиеся явные выборы могут сокращать ввод, но порядок кнопок, вкладок, секций и основных полей всегда остаётся стабильным.
12. **Не выдавать автоматику за выбор человека.** Программная подстановка и синтетическое событие не увеличивают частоту; учитывается только trusted-действие пользователя.
13. **Нейтральный фильтр остаётся нейтральным.** Для обычных структурированных фильтров статистика ранжирует варианты, но не включает скрытое ограничение выдачи.
14. **Предметная автоматика выше статистики.** Сохранённый объект, конкретный день календаря, найденный руководитель, вычисленный период плана и другая надёжная предметная логика не переопределяются learned default.
15. **Опасные решения не обучаются.** Роли, ACL, подтверждение/возврат/удаление, секреты, поиск и свободный деловой текст не получают частотных defaults.

Каждый повторяемый контрол относится к одному из режимов `safe-default / rank-only / domain-derived / never-learn`.

Общий приоритет безопасного значения:

```text
сохранённое значение существующего объекта / явный предметный контекст
        ↓
явный выбор пользователя в текущем действии
        ↓
надёжная domain-derived автоматика
        ↓
персональный самый частый safe-default
        ↓
статический безопасный fallback
```

Для `rank-only` накопленная статистика не выбирает значение: она меняет только порядок рабочих вариантов внутри списка.

Подробный allowlist, относительные даты и ограничения: [`ADAPTIVE_UX.md`](ADAPTIVE_UX.md).

## Сценарий 1. Ежедневная работа с календарём

Цель: увидеть текущую нагрузку без переходов по разделам.

1. Пользователь открывает систему.
2. Сразу видит текущий месяц либо свой наиболее частый явно выбранный календарный режим.
3. Сегодняшний день выделен акцентом.
4. Справа показаны записи на сегодня, ближайшие семь дней и вопросы проверки.
5. Переключение на неделю сохраняет текущую дату.
6. Переключение на задачи показывает просроченные, сегодняшние, предстоящие и выполненные задачи.

Критерии приёмки:

- календарь является активной панелью в исходном HTML;
- переключатель видов находится в постоянном месте верхней панели;
- частота выбора режима не переставляет кнопки `Месяц / Неделя / Задачи`;
- программно выбранный стартовый режим не записывается как ручной выбор;
- изменение вида не меняет текущий раздел;
- кнопка `Сегодня` возвращает текущий период;
- отсутствие записей объясняется текстом, а не пустой областью.

## Сценарий 2. Создание события или задачи

1. Пользователь нажимает `Создать` или `+` внутри дня.
2. Открывается лист, связанный с текущим календарём.
3. При общей кнопке `Создать` дата может получить безопасный персональный relative default (`d:N`) от текущего дня; при нажатии `+` дата берётся из конкретного выбранного дня и имеет более высокий приоритет.
4. Для нового объекта система может предложить наиболее часто явно выбранные этим пользователем тип, категорию, важность и напоминание.
5. Пользователь при необходимости меняет любое предложенное значение.
6. После сохранения запись появляется в текущем представлении без перезагрузки страницы.
7. Для задачи доступно завершение одним действием.

Критерии приёмки:

- обязательны только название и дата;
- значения по умолчанию безопасны и полностью редактируемы;
- обучаемая дата хранится как относительное смещение, а не как старый абсолютный `YYYY-MM-DD`;
- клик по конкретному дню всегда сильнее learned date offset;
- автоматически предложенное и не изменённое человеком значение не увеличивает свою частоту;
- успешный явный выбор учитывается для следующих новых действий;
- один нативный выбор, породивший `input` и `change`, учитывается один раз;
- редактирование существующей записи всегда показывает её сохранённые значения, а не статистический default;
- отмена не создаёт запись и не обучает предпочтение;
- повторное открытие показывает сохранённые значения;
- завершённая задача исчезает из активных уведомлений;
- задача может быть возвращена в работу.

## Сценарий 3. Уведомления и напоминания

1. Пользователь видит количество непрочитанных уведомлений на кнопке в верхней панели.
2. Открывает компактный центр уведомлений, не покидая календарь.
3. Уведомление объясняет, что произошло и к какой записи относится.
4. Пользователь отмечает его прочитанным или скрывает.
5. Просроченная задача получает отдельное уведомление высокой важности.

Правила:

- обычное событие без настроенного напоминания сообщает о себе только в день события;
- напоминание показывается не раньше выбранного срока;
- выполненные и отменённые записи не уведомляют;
- прочтение и скрытие сохраняются на сервере;
- уведомления не используют модальные предупреждения.

## Сценарий 4. Создание шаблона из одного документа

Цель: один раз показать поля и больше не размечать документы того же вида вручную.

1. Пользователь загружает документ обычным способом.
2. После извлечения текста нажимает `Создать шаблон` в строке документа.
3. Мастер открывает текст документа, не изменяя оригинал.
4. Пользователь щёлкает строку с нужным значением.
5. Система предлагает подпись поля и правило извлечения.
6. Пользователь задаёт понятное название и тип: текст, дата, число или да/нет.
7. Повторяет действие для остальных полей.
8. На этапе проверки система применяет шаблон к исходному документу и показывает значения и пропуски.
9. Только после проверки доступно осмысленное сохранение.
10. Следующий похожий документ автоматически сопоставляется по обязательным фразам и извлекается worker-процессом.

Поддерживаемые правила первого контура:

- значение после подписи в той же строке;
- следующая непустая строка;
- вся строка;
- текст между начальной и конечной подписями.

Критерии приёмки:

- мастер состоит из этапов `Документ / Поля / Проверка`;
- нельзя перейти к проверке без поля;
- нельзя сохранить шаблон без имени и поля;
- результат проверки отображается до сохранения;
- обязательный пропуск не уничтожает найденные значения, а создаёт один понятный вопрос оператору;
- шаблон хранит источник, версию, правила сопоставления и доказательства по строкам;
- повторно используемые безопасные вид документа, тип поля, стратегия и состояние `Поле обязательно` могут получать learned fallback;
- конкретная детерминированная эвристика (`Дата:`, `после подписи` и т.п.) сильнее общей статистики;
- название шаблона, произвольный текст и содержимое документа не используются для автоматического обучения;
- применение к повторному документу проверяется автоматическим тестом.

## Сценарий 5. Пакетная загрузка

1. Пользователь выбирает несколько файлов.
2. Баннер показывает текущий файл и общий прогресс.
3. Ошибка одного файла не останавливает остальные.
4. После приёма файлов пользователь может закрыть баннер или перейти в календарь.
5. Состояния документов обновляются фоновым опросом.

## Автоматические проверки

Release gate должен включать:

- синтаксическую проверку всех модулей;
- тест календарного напоминания и завершения задачи;
- тест детерминированных стратегий извлечения;
- тест сохранения шаблона и применения к следующему документу;
- статический UX-контракт главного календарного вида;
- статический UX-контракт трёх этапов мастера;
- существующий сквозной путь загрузки протокола;
- unit/integration для preference allowlist, account isolation, относительных дат и идемпотентного `interactionId`;
- upgrade схемы 17→18 без потери предметных данных;
- Playwright desktop/mobile: календарный default, реальный ручной выбор даты/типа, приоритет существующего объекта и конкретного дня, periodic-task, rank-only фильтр, дедупликация нативного `input+change`, manager/admin profile и неизменная геометрия;
- backup/restore и полный offline/systemd gate.

## Закреплённые UX-механизмы

Следующие механизмы уже реализованы и остаются обязательной частью регрессии:

1. Карточка события и документа в инспекторной панели без потери текущего контекста.
2. Перетаскивание событий с обратимым изменением даты.
3. Видимые календарные фильтры с сохранением состояния и нейтральные `rank-only` фильтры в обычных списках.
4. Структурные доказательства для DOCX/XLSX/PDF и ручное исправление без уничтожения машинного результата.
5. Автосохранение и восстановление черновика мастера шаблона.
6. Реальные desktop/mobile Playwright-сценарии в CI без включения Playwright в промышленный runtime.

## Сценарий 6. Инспектор без потери контекста

1. Пользователь нажимает событие, задачу или название документа.
2. Справа открывается инспектор; календарь или список остаётся на месте.
3. Инспектор показывает состояние, источник, доказательства и доступные действия.
4. На мобильном экране инспектор открывается снизу.
5. Закрытие возвращает фокус в исходный контекст.

Критерии приёмки:

- просмотр не открывает новую страницу;
- автоматически созданная запись показывает исходный документ;
- документ показывает поля шаблона и строки доказательств;
- редактирование использует прежнюю форму и терминологию;
- Esc закрывает инспектор раньше основного экрана.

## Сценарий 7. Перенос даты и отмена

1. Пользователь переносит запись на другой день.
2. Ручная запись перемещается сразу.
3. Для даты из документа система один раз предупреждает о смене исходного значения.
4. После переноса показывается действие `Отменить`.
5. Сервер хранит предыдущую и новую версии записи.

Критерии приёмки:

- время сохраняется при переносе на другой день;
- перенос не создаёт дубликат;
- отмена восстанавливает все поля предыдущей версии;
- каждое изменение записывается в аудит;
- завершённые задачи остаются доступными в истории.

## Сценарий 8. Восстановление черновика шаблона

1. Оператор начал разметку и закрыл браузер.
2. Поля, шаг и настройки сопоставления сохраняются автоматически.
3. При повторном открытии система предлагает продолжить или начать заново.
4. Восстановление повторно создаёт поля в рабочем состоянии мастера.
5. После успешного сохранения шаблона черновик удаляется.

Критерии приёмки:

- черновик хранится на сервере, с локальным резервом в браузере;
- сохранение выполняется без отдельной кнопки;
- черновик привязан к версии документа;
- черновик другого документа не подмешивается;
- удаление черновика не удаляет исходный документ.

## Сценарий 9. Проверка источника и исправление поля

1. Оператор открывает документ в инспекторе.
2. Ниже карточки отображается структурный источник: абзацы, ячейки либо страница PDF.
3. У извлечённого поля оператор нажимает `Показать источник`.
4. Система выделяет фрагмент, на котором основано значение.
5. Оператор может выбрать другой фрагмент и нажать `Исправить`.
6. Новое значение, причина и доказательство сохраняются отдельно от машинного результата.

Критерии приёмки:

- исходный автоматический результат не изменяется;
- ручное значение имеет отдельную отметку;
- у исправления есть пользователь, время и причина;
- DOCX показывает абзац или ячейку;
- XLSX показывает лист и адрес ячейки;
- PDF показывает страницу и положение строки;
- повторное открытие показывает актуальное ручное значение.

## Сценарий 10. Браузерная проверка desktop/mobile

Release gate использует реальный Chromium:

- desktop 1440×980;
- мобильный профиль iPhone;
- загрузка документа через пользовательский интерфейс;
- ожидание фоновой обработки через API;
- открытие инспектора;
- проверка структурного источника;
- проверка мобильного нижнего листа;
- общий обучаемый UX и его приоритеты.

Трассировка и снимок сохраняются при ошибке. Браузерные зависимости устанавливаются только в CI.

## Сценарий 11. Заседание кафедры → протокол → выписка

1. Секретарь открывает раздел `Заседания`.
2. При первом запуске система просит только обязательные параметры: шаблон протокола, шаблон выписки, кворум, председателя и секретаря. Председатель и секретарь выбираются из действующих сотрудников.
3. Если сохранённого председателя или секретаря ещё нет, система может предложить наиболее часто явно выбранного этим пользователем сотрудника; существующая настройка всегда имеет приоритет.
4. Секретарь создаёт заседание, указывая дату и номер протокола. Дата нового заседания может получить безопасный относительный default, но уже сохранённая дата не заменяется статистикой. Остальные параметры копируются из настроек кафедры.
5. Повестка собирается из задач и пунктов планов либо вручную. Система присваивает вопросы `1…N`; формулировку можно сразу исправить.
6. Для каждого вопроса секретарь заполняет фактические поля `Слушали`, `Обсудили`, `Решили`. Система не подставляет отсутствующих авторов, выступления или решения.
7. Полный протокол формируется одной командой по всей повестке.
8. Для выписки секретарь отмечает номера прямо в повестке, например 4 и 8. В документ попадают только эти вопросы с исходными номерами.
9. Сформированные DOCX остаются в документах заседания; повтор неизменённого запроса возвращает существующий документ.
10. Если вопрос создан из пункта плана, в плане показывается обратная ссылка `Рассмотрено на заседании кафедры: протокол №… от … · вопрос …`.

Критерии приёмки:

- настройка не требует общего мастера документов; оба DOCX загружаются прямо из раздела заседаний;
- вопрос из плана сохраняет связь с исходным пунктом и не изменяет его;
- удаление вопроса уплотняет отображаемую нумерацию;
- можно выбрать несмежные вопросы, в частности №4 и №8;
- выписка не включает неотмеченные вопросы;
- desktop и mobile выполняют один и тот же сценарий без скрытых обязательных действий;
- готовый протокол не импортируется повторно как второе заседание;
- ранее импортированные протоколы остаются доступными после миграции.

Подробный контракт шаблонов: [`MEETINGS.md`](MEETINGS.md).

## Сценарий 12. Повторяющийся выбор → безопасная адаптация по всему интерфейсу

Цель: сокращать повторный ввод без скрытого изменения данных, фильтров или расположения элементов управления.

1. Каждый контрол заранее классифицируется как `safe-default`, `rank-only`, `domain-derived` или `never-learn`.
2. В `safe-default` пользователь несколько раз явно выбирает допустимое значение в аналогичном контексте и успешно завершает основное действие.
3. Система хранит персональную частоту по рабочему пространству, аккаунту и смысловому контексту.
4. При следующем безопасном новом действии частый вариант может стать default; пользователь может сразу выбрать другой.
5. В `rank-only` статистика только переставляет рабочие варианты внутри списка: текущее и нейтральное значение остаются без изменения.
6. `domain-derived` значение и сохранённое поле существующего объекта имеют приоритет над статистикой.
7. `never-learn` вообще не передаётся в preference-layer.

Примеры текущей реализации:

- пользователь чаще выбирает `Неделя` → календарь может стартовать в режиме `Неделя`, но кнопки остаются в порядке `Месяц / Неделя / Задачи`;
- пользователь обычно создаёт научную задачу с высокой важностью, напоминанием за сутки и сроком `+7 дней` → эти безопасные значения могут предлагаться новой записи;
- пользователь нажал `+` на 18 августа → дата 18 августа выше learned `+7 дней`;
- Иванов часто выбирается исполнителем новой периодической задачи → Иванов может стать default исполнителя, но автоматически найденный по структуре руководитель не заменяется статистикой;
- в фильтре поиска часто выбирают `Распоряжения` → этот вариант поднимается в списке, но фильтр при открытии остаётся `Все`;
- частые вид периода и направление применяются к новой периодической задаче, а `periodKey` вычисляется из текущей даты, а не берётся из старого текста;
- тип/стратегия нового поля шаблона могут обучаться, но конкретная эвристика текущего документа сильнее общего fallback;
- manager/admin может восстановить частый рабочий профиль; `staff` всегда остаётся профилем собственного аккаунта.

Критерии приёмки:

- автоматически подставленное значение, которое пользователь не менял, не увеличивает частоту;
- synthetic DOM-событие не считается действием человека;
- один физический нативный выбор, породивший `input + change`, считается один раз;
- отменённая или неуспешная предметная форма не обучает систему;
- повторная доставка одного `interactionId` не удваивает частоту;
- статистика одного аккаунта не влияет на другой аккаунт;
- абсолютная дата не хранится как learned default: используется ограниченный `d:N`;
- конкретный день календаря, existing object и надёжное domain-derived значение выше learned default;
- обычный структурированный фильтр сохраняет нейтральное состояние и только ранжирует варианты;
- список может менять порядок рабочих вариантов внутри себя, но не размеры/положение формы, кнопок, вкладок и секций;
- серверный allowlist не допускает пароли, токены, логины, поиск, свободный деловой текст, роли, ACL и решения с последствиями;
- отказ preference API не мешает завершить основное действие;
- desktop/mobile выполняют одинаковый предметный сценарий.

Подробности: [`ADAPTIVE_UX.md`](ADAPTIVE_UX.md).