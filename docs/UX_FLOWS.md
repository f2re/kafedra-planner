# Пользовательские сценарии и UX-контракт

Статус: ежедневные контуры текущего `0.1.0-rc.6` реализованы и поддерживаются browser release-gates. Открытые планы развития не должны описываться здесь как готовые возможности.

## Общие принципы

1. **Сразу к работе.** После входа пользователь попадает в календарь.
2. **Стабильная геометрия.** Частота использования не переставляет кнопки, вкладки и основные секции.
3. **Явная настройка сильнее обучения.** Постоянная настройка стартового режима календаря имеет приоритет над learned UX.
4. **Предметные данные сильнее статистики.** Сохранённая дата, выбранный день, вычисленный период, найденный руководитель и другие domain-derived значения не заменяются learned default.
5. **Только явный выбор человека.** Программная подстановка и synthetic DOM events не обучают preference-layer.
6. **Безопасная адаптация.** Контролы классифицируются как `safe-default / rank-only / domain-derived / never-learn`.
7. **Не блокировать основной сценарий.** Ошибка preference-layer, внешней доставки или LLM не мешает предметному сохранению.
8. **Минимум модальных окон.** Формы открываются в листах/инспекторе и сохраняют введённые значения при временной ошибке.
9. **Происхождение видно рядом с результатом.** Документ, план, календарная задача и отчёт должны вести к источнику/evidence.
10. **Desktop/mobile — один предметный сценарий.** На мобильном меняется компоновка, а не смысл действий.

Подробный preference-контракт: [`ADAPTIVE_UX.md`](ADAPTIVE_UX.md).

## 1. Старт календаря

Пользователь может выбрать постоянную настройку:

```text
Автоматически / Месяц / Неделя / Задачи
```

- `Месяц`, `Неделя`, `Задачи` открываются всегда, пока пользователь не изменит настройку;
- `Автоматически` разрешает безопасный learned default/fallback;
- обычное переключение текущего вида не переписывает постоянную настройку;
- порядок кнопок остаётся фиксированным;
- настройка account-scoped при авторизации и имеет безопасный local fallback без auth.

Подробно: [`CALENDAR_START_MODE.md`](CALENDAR_START_MODE.md).

## 2. Создание события или задачи

1. Пользователь нажимает `Создать` либо `+` внутри конкретного дня.
2. Форма открывается в контексте календаря.
3. Конкретный выбранный день имеет приоритет над learned relative-date default.
4. Безопасные тип/категория/важность/напоминание могут предлагаться как personal default только для нового объекта.
5. После успешного сохранения запись появляется без перезагрузки.
6. Задачу можно завершить и вернуть в работу.

Редактирование существующей записи всегда показывает её сохранённые значения, а не статистические defaults.

## 3. Уведомления

- внутренний центр уведомлений работает без внешних сервисов;
- уведомление объясняет объект и требуемое действие;
- выполненные/отменённые обязательства не продолжают спамить;
- read/hide сохраняются серверно;
- SMTP/Telegram являются дополнительными каналами и не заменяют внутреннее уведомление;
- повторный worker run не создаёт дубликат одного notification key.

Подробно: [`NOTIFICATION_DELIVERY.md`](NOTIFICATION_DELIVERY.md).

## 4. Документ → обработка → источник

1. Пользователь загружает один или несколько файлов.
2. Каждый файл сохраняется до фоновой обработки как immutable version.
3. Ошибка одного файла не останавливает остальные.
4. Worker извлекает text/structure, при необходимости локально использует OCR/LibreOffice.
5. В инспекторе видны исходник/preview, поля и evidence.
6. Ручное исправление хранится отдельной ревизией с причиной.

Подробно: [`STRUCTURED_DOCUMENTS.md`](STRUCTURED_DOCUMENTS.md), [`OCR_AND_PREVIEW.md`](OCR_AND_PREVIEW.md).

## 5. Шаблон документа

1. Оператор выбирает исходный документ.
2. Размечает поддержанные поля/структурные якоря.
3. Проверяет извлечение до сохранения.
4. Неоднозначность показывается как исправимый вопрос, а не техническая ошибка.
5. Исходный документ не изменяется.
6. Черновик мастера может восстанавливаться без создания предметного результата.

Безопасные repeatable настройки поля могут использовать preference-layer; имя шаблона и свободный деловой текст не обучаются.

## 6. Годовой план

План можно импортировать либо создать вручную.

### Импорт

```text
immutable документ → plan → plan_items → календарные проекции
```

Однозначные пункты импортируются независимо; проблемная строка не блокирует остальные. У каждой извлечённой записи сохраняется источник.

### Ручной план

1. Пользователь создаёт календарный/учебный план.
2. Добавляет пункт из карточки либо через `В план` в календаре.
3. Выбирает режим:
   - `track` — контрольная точка;
   - `assigned` — поручение выбранным исполнителям;
   - `open` — открытая задача для самостоятельного принятия.
4. Один `plan_item` имеет не более одного связанного assignment.
5. Прогресс/отчёт/approval используют общий рабочий контур.
6. Подтверждённое выполнение синхронизирует план, календарь и `План / факт`.

Подробно: [`PLANS.md`](PLANS.md), [`MANUAL_PLANS.md`](MANUAL_PLANS.md).

## 7. Поручение → отчёт → подтверждение

1. Поручение возникает из основания либо пункта плана.
2. Исполнитель сохраняет прогресс и прикладывает/выбирает отчётный документ.
3. Автоматика может предложить связь, но не закрывает обязательство молча.
4. Руководитель подтверждает либо возвращает на доработку.
5. История, evidence и прежние отчёты сохраняются.
6. Результат отражается в `План / факт`.

Подробно: [`WORKFLOW_DOCUMENTS.md`](WORKFLOW_DOCUMENTS.md), [`PLAN_FACT.md`](PLAN_FACT.md).

## 8. Заседание кафедры

1. Секретарь создаёт заседание и указывает номер/дату.
2. Повестка формируется вручную, из пункта плана или задачи.
3. Вопросы имеют стабильную нумерацию.
4. Для каждого вопроса заполняются фактические `Слушали / Обсудили / Решили`.
5. Полный протокол либо выписка по выбранным вопросам формируются в DOCX.
6. Повтор неизменённой генерации не создаёт второй документ.
7. Пункт плана получает обратную ссылку на заседание/протокол/вопрос.

Подробно: [`MEETINGS.md`](MEETINGS.md).

## 9. «План / факт»

- руководитель/сотрудник видит только разрешённую сервером выборку;
- плановое и фактическое значения раскрываются до источника;
- ручная корректировка требует причины и не уничтожает machine value;
- сохранённые views не расширяют ACL;
- CSV/JSON отражают текущую серверную выборку;
- риск срока вычисляется детерминированно.

Подробно: [`PLAN_FACT_OPERATIONS.md`](PLAN_FACT_OPERATIONS.md).

## 10. Инспектор и восстановимость

- просмотр объекта не уводит пользователя из основного контекста;
- desktop использует боковой инспектор, mobile — нижний лист;
- Esc/закрытие возвращает фокус;
- обратимые действия предлагают простой undo/return path;
- формы не очищаются при временной ошибке;
- успешная загрузка файла не повторяется только потому, что последующая связь временно упала.

## 11. Обучаемый UX без скрытых решений

Примеры допустимого поведения:

- в `Автоматически` часто выбранная `Неделя` может стать стартовым видом;
- частая категория новой задачи может стать safe default;
- частый вариант фильтра может подняться в списке, но фильтр остаётся нейтральным;
- конкретный день календаря всегда сильнее learned date offset;
- найденный по структуре руководитель сильнее статистики;
- пароль, токен, поиск, свободный текст, роль, ACL, approve/return/delete никогда не обучаются.

Приоритет:

```text
сохранённое значение / явный предметный контекст
→ явный выбор текущего действия
→ domain-derived значение
→ безопасный personal default
→ статический fallback
```

## 12. Release gate UX

Browser CI проверяет фактические package scripts:

```bash
npm run test:browser:plans
npm run test:browser:core
npm run test:browser:reports-science
npm run test:browser:plan-fact
npm run test:browser:auth
npm run test:browser:release
npm run test:browser:acl
```

Проверяются desktop и mobile, сохранение пользовательского ввода, object access, стабильный календарный старт, планы, заседания и release readiness. Browser CI дополняет, но не заменяет full-offline/systemd и целевую Astra-приёмку.
