# План развития

## Текущий рубеж — `0.1.0-rc.3`

Реализованы основные вертикальные контуры: документы и доказательства, календарь и задачи, распоряжения и поручения, отчётность и план/факт, научный реестр, локальная авторизация и объектные ACL, безопасное обновление/восстановление, планы работы и персональная внешняя доставка уведомлений.

До стабильного релиза программно незакрытым считается прежде всего эксплуатационный контур R8: реальная установка и восстановление на целевой Astra Linux/Debian. Функциональность, которую невозможно честно подтвердить только Ubuntu CI, не считается принятой до #27.

## R0.6 — основания, поручения и отчётность

Состояние: рабочий вертикальный контур реализован; расширение golden-корпуса продолжается.

- указы, распоряжения и приказы как основания;
- поручения, исполнители, контролирующие и сроки;
- календарные задачи и уведомления сотруднику/руководителю;
- семестровые и годовые задачи сотрудников;
- отчётные документы и история выполнения;
- фасетный поиск по номеру, периоду, направлению и исполнителю;
- полностью рабочий детерминированный режим;
- необязательное локальное обогащение через llama.cpp без автоматической подмены фактов.

Критерий: каждое обязательство связано с исходным документом и доводится до отчёта и подтверждения без потери истории.

## R0 — платформенное основание

Состояние: реализовано в `0.1.0-alpha.1` и далее укреплено release-gates.

- Node.js API и worker;
- SQLite, WAL, миграции и проверка целостности;
- неизменяемое blob-хранилище;
- устойчивая очередь;
- DOCX, ODT, PDF и текст;
- протоколы, решения, календарь, FTS5;
- операторский интерфейс;
- автономный bundle skeleton.

## R1 — безопасная эксплуатация

Состояние: основной release-контур реализован; целевая приёмка файловой системы и systemd относится к R8.

- первый администратор;
- локальные пользователи и параметризованный `scrypt` из штатного `node:crypto`;
- роли сотрудника, руководителя и администратора;
- HttpOnly-сессии и CSRF;
- серверная привязка рабочего пространства;
- аудит значимых изменений;
- объектные ограничения доступа к документам и производным сущностям;
- резервная копия и проверяемое восстановление;
- административная диагностика готовности.

`Argon2id` не является обязательным runtime-требованием: текущий выбор `scrypt` сделан осознанно, чтобы не вводить нативную npm-зависимость в автономную Astra-поставку. Параметры хэша хранятся вместе с записью, поэтому алгоритм/стоимость могут быть мигрированы позже без сброса всех аккаунтов.

Критерий: система выдерживает перезапуск, обновление и восстановление без потери подтверждённых данных.

## R2 — операторская работа с документом

- карточка документа;
- просмотр исходного DOCX/PDF;
- подсветка фрагмента-доказательства;
- выбор типа документа;
- ручное исправление извлечённого значения без уничтожения машинного;
- сопоставление людей;
- версии подтверждённых фактов;
- повторный разбор новой версией шаблона.

Критерий: любой спорный результат исправляется на месте без повторной загрузки пакета.

## R3 — шаблоны

- декларативный контракт полей;
- визуальный выбор якоря и повторяемого блока;
- поля и типы значений;
- трансформации без пользовательского кода;
- черновики, проверка и повторное применение;
- структурные локаторы;
- дальнейшее расширение корпуса примеров и ранжирования шаблонов.

## R4 — люди и организация

- люди, варианты ФИО и устойчивые идентификаторы;
- иерархия руководителей;
- осторожное сопоставление однофамильцев;
- участие в заседаниях;
- ответственные и контролирующие поручений;
- персональная лента задач, план/факт и уведомлений.

Дальнейшее развитие: полноценные подразделения и должности с периодами действия.

## R5 — наука

Рабочий первый срез реализован:

- публикации;
- DOI и внутренние идентификаторы;
- авторы;
- ВАК, РИНЦ, МБД и настраиваемые классификации;
- конференции, проекты, заявки, патенты и отчёты НИР;
- подтверждающие документы;
- фасетный поиск.

Дальнейшее развитие: более глубокие аффилиации, статусы жизненного цикла и агрегированные отчёты по человеку/кафедре/периоду.

## R6 — образование

Следующий самостоятельный продуктовый цикл после stable:

- дисциплины и учебные мероприятия;
- учебно-методические материалы;
- практики, ГИА, комиссии;
- импорт результатов `planer-solving`;
- календарные рубежи;
- отчёты по нагрузке и участию.

## R7 — интеграции и выпуск документов

Реализовано как платформенное основание:

- transactional outbox внешних уведомлений;
- idempotency keys для критических повторяемых операций;
- SMTP без обязательной npm runtime-зависимости;
- Telegram как необязательный адаптер.

Следующие интеграции:

- стабильный OpenAPI;
- Docomator API;
- Planner Solving API;
- iCalendar;
- сетевая папка;
- LDAP/FreeIPA.

## R8 — целевая поставка Astra

Состояние: реализация поставки существует, целевая эксплуатационная приёмка #27 не пройдена.

- точные профили Astra Linux 1.7 и 1.8;
- закреплённый Node.js 24 runtime;
- `unzip`, `poppler-utils`, LibreOffice, Tesseract;
- полная замкнутость пакетов;
- install/update/rollback;
- тест реальной конвертации;
- акт резервного восстановления;
- UX-приёмка без инструкции.

## R0.7 — отчёты и научный реестр

Состояние: рабочий вертикальный срез реализован.

- автоматическое сопоставление отчёта с поручением;
- подтверждение результата руководителем или возврат;
- персональные аудитории уведомлений;
- локальное извлечение научных материалов;
- общий реестр и фасетный поиск науки.

Критерий: поручение закрывается только подтверждённым результатом, а научная публикация не теряется после загрузки документа.

## R0.8 — план/факт и персональный контроль

Состояние: рабочий вертикальный срез реализован.

- извлечение плановых показателей из поручений;
- извлечение фактических показателей и состояния из отчётов;
- несколько отчётов и сохранение ревизий результатов;
- подтверждённый план/факт по поручению;
- персональная выборка сотрудника и контроль руководителя;
- детерминированная оценка риска срока;
- раздельное состояние уведомлений для каждого сотрудника.

Критерий: по каждому обязательству видны план, фактический результат, доказательство, подтверждение и ответственный адресат предупреждения.

## R0.9 — планы работы и календарная проекция

Состояние: этап завершён в #31/#32/#33/#37.

Реализовано:

- отдельные сущности плана и пункта плана без смешивания с контуром `plan-fact`;
- виды: кафедра, факультет, личный, подразделение, организация;
- календарный и учебный период с наследованием года для неполных дат;
- детерминированный разбор таблиц DOCX/ODT/XLSX/ODS и текстовых представлений;
- точные даты, месяцы, кварталы и контрольные сроки;
- частичный импорт: неоднозначная строка не блокирует остальные пункты;
- доказательства по исходной версии документа и локатору строки/ячейки;
- точное, не предположительное сопоставление ответственных;
- идемпотентное сохранение по версии документа;
- FTS и фасеты по виду, периоду, направлению и ответственному;
- календарная проекция `plan_item` с явным происхождением;
- наследование ACL от исходного документа;
- сохранение DOCX-образца как неизменяемого источника шаблона;
- автоопределение учебного/календарного года, таблицы, строки-образца и колонок;
- остановка автогенерации при неоднозначной структуре;
- безопасная автоподстановка года только в подтверждённых абзацах;
- клонирование OOXML-строки с сохранением стилей и заполнением пунктов;
- детерминированный ZIP writer без npm-зависимости и без команды `zip`;
- идемпотентный журнал генераций с восстановлением после сбоя;
- регистрация сформированного DOCX через обычный document intake и повторный worker-разбор;
- раздел `Планы` с фильтрами по виду, периоду, направлению, ответственному и тексту;
- карточка плана с исходным документом, пунктами и локаторами доказательств;
- обратимое исправление пункта без изменения `evidence_json`;
- атомарное обновление календаря, FTS, фасетов и вопроса проверки после коррекции/отмены;
- ручной выбор года/таблицы при неоднозначном DOCX вместо предположения системы;
- формирование следующего периода из интерфейса с предварительной проверкой перенесённых дат;
- человекочитаемый источник плановой записи в календаре и переход обратно к плану;
- сквозные тесты `import → plan → calendar`, `template → DOCX → worker → plan → calendar` и correction/undo;
- backup/restore self-test проходит на текущей схеме 15.

Приёмка этапа:

- Playwright desktop/mobile: DOCX → план → следующий период → календарь → источник;
- Playwright desktop/mobile: готовый план → неоднозначный срок → исправление → отмена;
- полная регрессия календаря, документов, авторизации, ACL, отчётов, науки и план/факта.

Критерий этапа выполнен: загруженный или сформированный план воспроизводимо превращается в проверяемые пункты и календарные рубежи; спорный пункт исправляется и отменяется без потери исходного доказательства; каждый производный объект трассируется к исходному документу/шаблону и не расширяет права доступа.

## R0.10 — персональная доставка уведомлений

Состояние: реализовано в #25.

- внутренний центр уведомлений остаётся обязательным автономным каналом;
- SMTP/STARTTLS/SMTPS реализованы штатными средствами Node.js;
- Telegram подключается только как необязательный адаптер;
- персональные каналы связаны с серверной сессией сотрудника;
- немедленная доставка, daily/weekly digest и quiet hours;
- руководитель получает только предусмотренную проверку/эскалацию риска, а не копию каждого напоминания;
- outbox и job создаются атомарно;
- один `notification_key` создаёт не более одного сообщения на сотрудника и канал;
- автоматические повторы завершаются до доступности ручного retry;
- выключенная внешняя доставка ставит delivery jobs на паузу, но не блокирует документы;
- приоритет внешней доставки ниже `process_document`;
- административная диагностика показывает терминальные ошибки и позволяет повторить отправку;
- SMTP relay acceptance, Telegram acceptance и подтверждение пользователем имеют разные статусы;
- секреты провайдеров не сохраняются в БД или аудит.

Критерий этапа: внешний канал не является условием работы системы; один сбой канала не блокирует worker; повторная обработка не создаёт второй outbox; сотрудник и руководитель получают только предназначенные им сообщения.
