name: Release gate 0.3.4

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: release-gate-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  quality:
    name: release-quality
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - run: npm run check
      - run: npm test
      - run: npm run smoke

  migrations:
    name: release-migrations-backup
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - name: Schema 19 → current and release evidence
        run: >-
          node --test
          tests/organization-migration.test.mjs
          tests/science-lifecycle-migration.test.mjs
          tests/science-import-migration.test.mjs
          tests/science-reports-migration.test.mjs
          tests/plan-source-rows-migration.test.mjs
          tests/docomator-integration-migration.test.mjs
          tests/docomator-integration.test.mjs
          tests/docomator-fields-migration.test.mjs
          tests/docomator-fields.test.mjs
          tests/lifecycle.test.mjs
          tests/acceptance-020.test.mjs
          tests/backup-restore.test.mjs
          tests/target-acceptance.test.mjs

  browser:
    name: release-browser-desktop-mobile
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: npm
      - run: npm ci --ignore-scripts --no-audit --no-fund
      - run: npx playwright install --with-deps chromium
      - name: Complete 0.3.4 scenarios
        run: >-
          npx playwright test
          tests/browser/organization.spec.mjs
          tests/browser/docomator-integration.spec.mjs
          tests/browser/docomator-fields.spec.mjs
          tests/browser/meeting-template-editor.spec.mjs
          tests/browser/manual-plan-preferences.spec.mjs
          tests/browser/science-lifecycle.spec.mjs
          tests/browser/science-import.spec.mjs
          tests/browser/science-reports.spec.mjs
          tests/browser/plan-source-rows.spec.mjs
          tests/browser/lifecycle.spec.mjs
          tests/browser/auto-assignment.spec.mjs

  release-gate:
    name: release-gate
    if: always()
    needs: [quality, migrations, browser]
    runs-on: ubuntu-24.04
    steps:
      - name: Require every release job
        env:
          QUALITY: ${{ needs.quality.result }}
          MIGRATIONS: ${{ needs.migrations.result }}
          BROWSER: ${{ needs.browser.result }}
        run: |
          printf 'quality=%s\nmigrations=%s\nbrowser=%s\n' "$QUALITY" "$MIGRATIONS" "$BROWSER"
          test "$QUALITY" = success
          test "$MIGRATIONS" = success
          test "$BROWSER" = success
